const appBarHeight = "40px";

export const Layout = {
  appBarHeight: appBarHeight,
  bodyHeight: `calc(100vh - ${appBarHeight})`,
  bodyMinWidth: "340px",
  bodyMaxWidth: "1240px",
};

export const modalSize = "300px";
export const modalMinimumSize = modalSize;
